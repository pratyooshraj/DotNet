@model DbUsingADONET.Models.Department

@{
    ViewData["Title"] = "Department Details";
}

<h2>Department Details</h2>

<div>
    <h4>Department</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            Name
        </dt>
        <dd class="col-sm-10">
            @Model.DepartmentName
        </dd>
    </dl>
</div>
<div>
    <a href="@Url.Action("Edit", new { id = Model.DepartmentId })" class="btn btn-secondary">Edit</a> |
    <a href="@Url.Action("Index")" class="btn btn-secondary">Back to List</a>
</div>
